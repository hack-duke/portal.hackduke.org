version: "3.9"

services:
  db:
    image: postgres:16
    container_name: postgres_db
    restart: always
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432" 

volumes:
  pgdata: 