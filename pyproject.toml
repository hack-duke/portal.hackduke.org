[tool.poetry]
name = "portal-backend"
version = "0.1.0"
authors = ["rally.lin@duke.edu"]
description = ""
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.116.1"
pydantic = "^2.11.7"
uvicorn = "^0.35.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.4.2"